node_modules/
dist/
.next/
out/
# No bare `build/` here: nothing in this repo emits one (Next writes .next/,
# Nest writes dist/), and the pattern matched the /build route directory in the
# web app, silently keeping that page out of every deploy.

.env
.env.local
.env.*.local

*.tsbuildinfo
next-env.d.ts
.DS_Store

# Leftover from the old JSON-file agent store, replaced by Postgres.
data/

# Written by scripts/migrate-agents-to-primary.mjs, one line per moved agent.
apps/api/scripts/migration-mapping.csv
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
